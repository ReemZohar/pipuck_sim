#include <gtest/gtest.h>

namespace argos {
namespace {

TEST(PhybotControllerRoleTest, AssignsPaperInitialPressures) {
    GTEST_SKIP()
        << "Requires a deterministic ARGoS controller fixture with controllable local role observations.";
}

} // namespace
} // namespace argos
